# 개발 진행 업데이트 — 2026-03-30

## 전체 현황

| 상태 | 건수 | 비고 |
|------|------|------|
| Done | 4건 | D-01, D-02, C-02, HWP OCR |
| In Review | 2건 | D-03, C-02 |
| In Progress | 2건 | C-01, D-04 (추가 기능) |
| Todo | 28건 | |

---

## 완료 (Done)

### D-01 멀티 포맷 파싱 (FUT-455)

- PDF, DOCX, HWP, XLSX, CSV 파싱 구현 완료
- HWP: OLE2 바이너리 파서 직접 구현 (`_ureca` 모듈)
- HWPX: XML 기반 파서 구현
- CSV: pandas 기반 파서 구현
- PDF/DOCX: RAGAnything + Docling 활용

### D-02 의미론적 청킹 (FUT-456)

- SOP 구조 인식 청킹 알고리즘 구현
- 청크 토큰 크기: 500 (SOP 문서 최적화, 기본 1200 대신)
- 오버랩: 125 토큰
- 제목 계층 인식 및 문맥 보존

### C-02 엔티티/관계 추출 (FUT-459)

- LLM 기반 추출 (GPT-4o-mini, 한국어 프롬프트)
- 13종 엔티티 타입: 역할, 장비, 조직, 장소, 단계, 조건, 규칙, 주의사항, 문서, 소모품, 규격, 증상, 장애
- Graph CRUD MCP 도구 12종 구현 (조회/생성/수정/삭제/병합)
- 노이즈 필터링 및 동의어 정규화 (`postprocess.py`)

### HWP OCR (FUT-523)

- FUT-455 하위 태스크, HWP 바이너리 OCR 추가

---

## 리뷰 중 (In Review)

### D-03 메타데이터 추출 (FUT-457)

- 문서 메타데이터 자동 추출 구현 (classification, title, author, dates, keywords)
- 청크 단위 페이지 추적 (`page_indices`)
- References 메타데이터 보강 (F01)
- MCP 도구 2종 추가: `get_document_metadata()`, `update_document_metadata()`
- 코드 리뷰 완료, push 예정 (3/31)

**PRD 대비 구현 현황**:
- 규칙 기반 분류 (doc_type) — 구현 완료
- LLM 기반 키워드 추출 — 구현 완료
- 문서 통계 (pages, characters, tables, images, chunks) — 구현 완료

### C-02 엔티티/관계 추출 리뷰 (FUT-459)

- Graph CRUD 도구 코드 리뷰 완료, develop 브랜치 머지 대기

---

## 진행 중 (In Progress)

### C-01 온톨로지 정의 (FUT-458)

- 현재 13종 엔티티 타입 + 동의어 매핑 운용 중
- PRD 스펙(12종 제조 도메인 특화)과 현행 구현(13종) 간 용어 차이 존재 → 매핑 작업 필요
- FUT-459(Graph CRUD)와 통합 작업 진행 중

### D-04 레이아웃 레이블 활용 (FUT-640) — PRD 범위 외 추가 기능

- Docling Heron 레이아웃 모델의 17 class 레이블을 활용한 청크/문서 메타데이터 확장
- `inject_layout_labels()` 함수, `layout_labels` 청크 필드
- KV(Key-Value) 추출, header/footer 감지, 체크박스 변환
- 코드 리뷰 2라운드 완료 (3 리뷰어 × 2라운드), push 예정 (3/31)

---

## 인프라 관련

### LightRAG 업그레이드 (LKB-993)

- LightRAG 1.4.9.11 → 1.4.12 업그레이드
- UTC 타임스탬프 정규화, cooperative yield 패턴 적용
- push 예정 (3/31, D-03 이후)

### vLLM 자체 호스팅 인프라 검토

- 현재 embedding: OpenAI `text-embedding-3-small` (1536d)
- 검토 중인 대안: `dragonkue/BGE-m3-ko` via vLLM (1024d)
- 현재 rerank: Cohere API 무료 플랜 (월 1,000회 제한) → BGE-Reranker-v2-m3-ko 대체 검토
- vLLM 0.18.1rc 테스트 서버 (`vllmdev.linkbrain.ai.kr`)에서 embed 정상 동작 확인
- 전환 시 차원 변경(1536d→1024d)으로 전체 재인덱싱 필요

---

## 다음 단계 (크리티컬 패스)

```
3/31  FUT-457 (D-03) push → PR
       ↓
3/31  LKB-993 (LightRAG 업그레이드) push → PR
       ↓
3/31  FUT-640 (D-04) push → PR
       ↓
04-18 FUT-458 (C-01) 온톨로지 스키마 관리 체계 확립
       ↓
05-02 FUT-460 (C-03) 실체 연결 — 동의어/약어 통합
       ↓
05-30 FUT-462 (C-05) 요약 및 임베딩
```

## PRD 스펙 대비 참고사항

| 항목 | PRD 스펙 | 현행 구현 | 차이 |
|------|---------|----------|------|
| 엔티티 타입 | 12종 (Equipment, Process 등 제조 특화) | 13종 (역할, 장비 등 한국어 기반) | 용어 매핑 필요 |
| 임베딩 | text-embedding-3-large (1536d) | text-embedding-3-small (1536d) | 모델 크기 차이, 차원 동일 |
| 엔티티 추출 LLM | GPT-4o / Claude Sonnet | GPT-4o-mini | 비용 최적화 선택 |
| 청크 크기 | 300~800 토큰 | 500 토큰 (SOP 최적화) | 범위 내 |
| Reranking | 명시 없음 | Cohere rerank-v3.5 → BGE 전환 검토 | 추가 구현 |
